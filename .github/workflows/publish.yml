name: Publish
on:
  push:
    tags:
      - v*

jobs:
  build-and-test:
    uses: ./.github/workflows/build_and.test.yml
  publish:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run echo
        run: echo Publishing
