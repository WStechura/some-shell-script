name: Build and test
on:
  pull_request:
    branches: [ "release/*" ]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run echo
        run: echo Publishing
